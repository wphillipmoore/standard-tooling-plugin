# Memory Management — Human-Routed Writes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Enforce human approval before any memory write, with routing guidance so information lands in the correct scope (repo memory, global CLAUDE.md, or plugin/skill issue).

**Architecture:** Three prose-based enforcement layers — global CLAUDE.md policy (authoritative rule), MEMORY.md policy header (point-of-write trap), and two skills (memory-init for setup, memory-audit for ongoing review). No hooks; enforcement is instruction-based. Fleet rollout via per-repo GitHub issues.

**Tech Stack:** Markdown (CLAUDE.md, SKILL.md files), GitHub CLI (`gh`) for issue creation.

**Spec:** `docs/specs/2026-05-01-memory-management-design.md`

---

### Task 1: Add memory management policy to global CLAUDE.md

**Files:**
- Modify: `~/.claude/CLAUDE.md` (append new section after existing voice-to-text section)

- [ ] **Step 1: Read the current file to confirm structure**

Run: `cat ~/.claude/CLAUDE.md`
Expected: File ends after the voice-to-text section. No existing memory policy section.

- [ ] **Step 2: Append the memory management policy section**

Add this after the final line of the existing file:

```markdown

## Memory management

Never write to, edit, or delete memory files (`MEMORY.md` or any file
under the memory directory) without explicit human approval.

**When you want to save something to memory**, propose it first:

1. Tell the human what you would save and why.
2. Suggest where it belongs:
   - **Repo memory** — specific to this one codebase and only this
     codebase (e.g., a quirk of this repo's test setup).
   - **Global CLAUDE.md** — a cross-repo behavioral preference or
     convention (e.g., preferred error-handling style). This file.
   - **Plugin/skill issue** — about how the standard-tooling suite
     should behave (e.g., a skill needs updating). Create an issue
     in the appropriate repository.
3. Wait for the human to confirm or redirect before taking any action.

**Exemption:** User-invoked skills that write to memory as part of
their documented workflow (e.g., `/handoff stop`) have implicit
approval — the user triggered the skill. Never self-invoke an exempt
skill to bypass this policy; the exemption applies only when the
human initiates the skill invocation.
```

- [ ] **Step 3: Validate the file renders correctly**

Run: `markdownlint ~/.claude/CLAUDE.md` (if available) or visually confirm structure.

- [ ] **Step 4: Commit**

This file is outside the repo worktree and not under source control. No commit needed. Confirm the edit is saved.

---

### Task 2: Create the `memory-init` skill

**Files:**
- Create: `skills/memory-init/SKILL.md`

- [ ] **Step 1: Create the skill directory**

Run: `mkdir -p skills/memory-init`

- [ ] **Step 2: Write SKILL.md**

Create `skills/memory-init/SKILL.md` with this content:

```markdown
---
name: memory-init
description: Initialize or update the memory directory with the human-approval policy header in MEMORY.md.
---

# Memory Init

Set up the project memory directory with a policy header that requires
human approval before any memory write. Idempotent — safe to run
repeatedly.

## Behavior

1. **Resolve the memory directory.** The path is
   `~/.claude/projects/<slug>/memory/` where `<slug>` is derived from
   the session's starting CWD. Use the path the runtime provides — do
   not reimplement slug derivation.

2. **Ensure the directory exists.** Create it if missing.

3. **Create or update MEMORY.md.**
   - If `MEMORY.md` does not exist: create it with the policy header
     below and no index entries. Leave any pre-existing `.md` files
     in the directory as orphans — `memory-audit` handles those.
   - If `MEMORY.md` exists: compare the first block (everything before
     the first blank line after the closing `-->`) against the
     canonical header below. If it differs, replace the header,
     preserving all content after it.

## Canonical policy header

This is the source of truth. If the header in `MEMORY.md` does not
match this text exactly, replace it.

<!-- POLICY: Do not edit this file or any file in this directory without
explicit human approval. Before writing, propose the content to the human
and suggest where it belongs: repo memory, global CLAUDE.md, or a
plugin/skill issue. See the global CLAUDE.md memory management policy
for the full workflow. User-invoked skills (e.g., /handoff) are exempt. -->

## Empty MEMORY.md template

When creating a new `MEMORY.md`, use this structure:

<!-- POLICY: Do not edit this file or any file in this directory without
explicit human approval. Before writing, propose the content to the human
and suggest where it belongs: repo memory, global CLAUDE.md, or a
plugin/skill issue. See the global CLAUDE.md memory management policy
for the full workflow. User-invoked skills (e.g., /handoff) are exempt. -->

(no entries yet)

## Properties

- **Idempotent.** Running twice produces the same result.
- **Header only.** Never modifies index entries or other memory files.
- **No category scaffolding.** Future extension.
```

- [ ] **Step 3: Run markdownlint**

Run: `markdownlint skills/memory-init/SKILL.md`
Expected: No errors.

- [ ] **Step 4: Commit**

```bash
git add skills/memory-init/SKILL.md
st-commit --type feat --scope memory --message "add memory-init skill" --agent claude
```

---

### Task 3: Create the `memory-audit` skill

**Files:**
- Create: `skills/memory-audit/SKILL.md`

- [ ] **Step 1: Create the skill directory**

Run: `mkdir -p skills/memory-audit`

- [ ] **Step 2: Write SKILL.md**

Create `skills/memory-audit/SKILL.md` with this content:

```markdown
---
name: memory-audit
description: Collaborative review of memory files — verify, assess staleness, and route each entry to the correct scope with human approval.
---

# Memory Audit

Structured walk-through of a project's memory files. For each file,
verify its claims against the codebase, suggest a disposition, and
wait for the human to decide before acting.

## Workflow

### 1. Inventory

Read `MEMORY.md` and list all `.md` files in the memory directory.
Report:

- File count and breakdown by type (feedback, project, reference,
  user, handoff).
- Last-modified date for each file.
- Orphan files — present in the directory but not indexed in
  `MEMORY.md`.
- Broken links — entries in `MEMORY.md` pointing to files that no
  longer exist.

### 2. Walk-through

Present each memory file one at a time. For each:

**Show:** Name, type (from frontmatter), and content. Summarize if
the file exceeds ~30 lines.

**Verify:** Actively check claims against the codebase:
- If the memory references a file path, check it exists.
- If it references a function, pattern, or convention, grep for it.
- If it references a tool or command, check if it is on PATH.

Present verification findings alongside the content.

**Assess:** Based on verification results, is this entry still
accurate?

**Suggest disposition** with reasoning — one of:

| Disposition | When to use |
|---|---|
| **Keep** | Still accurate, correctly scoped to this repo. |
| **Update** | Substance is right, content needs refreshing. |
| **Relocate → global CLAUDE.md** | Cross-repo preference, not repo-specific. |
| **Relocate → plugin/skill issue** | About standard-tooling suite behavior. |
| **Delete** | Stale, redundant, or no longer relevant. |

### 3. Human decides

Wait for the human to confirm or override the suggestion. Do not
proceed until the human responds. One file at a time — no batching.

### 4. Execute

After the human decides:

- **Keep:** No change.
- **Update:** Edit the file. Show the diff before and after.
- **Relocate → global CLAUDE.md:** Draft the addition to
  `~/.claude/CLAUDE.md`. Show the draft. Write on approval.
- **Relocate → plugin/skill issue:** Draft the issue body. Show the
  draft. Create the issue with `gh issue create` on approval.
- **Delete:** Remove the file and its index entry from `MEMORY.md`.

### 5. Reconcile index

After all files are reviewed:

- Update `MEMORY.md` to reflect the final state (removed entries,
  newly indexed orphans, etc.).
- Ensure the policy header is intact. If missing or outdated, restore
  it per the `memory-init` skill's canonical header.

## Constraints

- **One repo per audit.** Operates on the current project's memory
  directory only.
- **No batch mode.** Every disposition requires human confirmation.
- **Policy header is sacred.** Never remove or modify the policy
  header except to update it to the canonical version.
```

- [ ] **Step 3: Run markdownlint**

Run: `markdownlint skills/memory-audit/SKILL.md`
Expected: No errors.

- [ ] **Step 4: Commit**

```bash
git add skills/memory-audit/SKILL.md
st-commit --type feat --scope memory --message "add memory-audit skill" --agent claude
```

---

### Task 4: Update this repo's CLAUDE.md — replace auto-memory policy

**Files:**
- Modify: `CLAUDE.md` (replace the "Auto-memory policy" section)

- [ ] **Step 1: Read the current auto-memory policy section**

Confirm the section to replace starts with `## Auto-memory policy` and ends before `## Parallel AI agent development`.

- [ ] **Step 2: Replace the section**

Replace:

```markdown
## Auto-memory policy

**Do NOT use MEMORY.md.** Never write to MEMORY.md or any file under the
memory directory. All behavioral rules, conventions, and workflow instructions
belong in managed, version-controlled documentation (CLAUDE.md, AGENTS.md,
skills, or docs/). If you want to persist something, tell the human what you
would save and let them decide where it belongs.
```

With:

```markdown
## Memory management

Memory is allowed with human approval. The authoritative policy is in
the user's global `~/.claude/CLAUDE.md` — agents must propose memory
writes and suggest a destination (repo memory, global CLAUDE.md, or
plugin/skill issue) before writing. See that file for the full
workflow.

Available skills:
- `/standard-tooling:memory-init` — set up or update the policy header
  in a project's `MEMORY.md`.
- `/standard-tooling:memory-audit` — structured collaborative review
  of memory files.
```

- [ ] **Step 3: Run markdownlint**

Run: `markdownlint CLAUDE.md`
Expected: No errors.

- [ ] **Step 4: Commit**

```bash
git add CLAUDE.md
st-commit --type docs --scope memory --message "replace memory ban with managed-memory policy" --agent claude
```

---

### Task 5: Add exemption note to handoff skill

**Files:**
- Modify: `skills/handoff/SKILL.md` (add note near the top of the Overview section)

- [ ] **Step 1: Read the current Overview section**

Confirm the Overview section describes the two modes (stop/start) and the purpose.

- [ ] **Step 2: Add the exemption note**

After the line `- `/handoff start` — Resume from the last handoff file after restarting.` and before `## Mode: stop`, insert:

```markdown

**Memory policy exemption.** This skill writes to the memory directory
as part of its documented workflow. Because the human invokes `/handoff`
explicitly, the write has implicit approval and is exempt from the
global memory management policy's approval requirement.
```

- [ ] **Step 3: Run markdownlint**

Run: `markdownlint skills/handoff/SKILL.md`
Expected: No errors.

- [ ] **Step 4: Commit**

```bash
git add skills/handoff/SKILL.md
st-commit --type docs --scope handoff --message "add memory policy exemption note" --agent claude
```

---

### Task 6: Remove phantom `block-memory-writes` hook references

**Files:**
- Modify: `CHANGELOG.md:201`
- Modify: `releases/v1.3.0.md:33-34,51`
- Modify: `releases/v1.3.1.md:33-34,51`
- Modify: `releases/v1.4.2.md:72,86`
- Modify: `docs/site/docs/hooks/index.md:221-224`

- [ ] **Step 1: Grep to confirm all references**

Run: `grep -rn "block-memory-writes" --include="*.md" --include="*.json"`
Expected: Matches in the five files listed above plus the design spec (which is a valid reference).

- [ ] **Step 2: Fix `docs/site/docs/hooks/index.md`**

Replace lines 221-224:

```markdown
## PreToolUse Hooks — Write|Edit

*(none currently active — `block-memory-writes` was removed on
2026-04-23 when feedback memory was re-enabled fleet-wide.)*
```

With:

```markdown
## PreToolUse Hooks — Write|Edit

*(none currently active)*
```

- [ ] **Step 3: Fix `releases/v1.3.0.md`**

Line 33-34 — the Documentation entry references `block-memory-writes removed` alongside `block-protected-branch-work now worktree-aware`. Update to remove the `block-memory-writes` part:

Replace:
```
- **cross-ref git-workflow guide and refresh hook entries (block-memory-writes removed, block-protected-branch-work now worktree-aware) (#48)**
* docs: cross-ref git-workflow guide and refresh hook entries (block-memory-writes removed, block-protected-branch-work now worktree-aware)
```

With:
```
- **cross-ref git-workflow guide and refresh hook entries (block-protected-branch-work now worktree-aware) (#48)**
* docs: cross-ref git-workflow guide and refresh hook entries (block-protected-branch-work now worktree-aware)
```

Line 51 — remove `block-memory-writes` from the hooks list. Replace:
```
- block-memory-writes: enforces auto-memory policy (no MEMORY.md writes)
```

Delete the line entirely.

- [ ] **Step 4: Fix `releases/v1.3.1.md`**

Same changes as v1.3.0.md — lines 33-34 and 51 have identical content.

Replace lines 33-34:
```
- **cross-ref git-workflow guide and refresh hook entries (block-memory-writes removed, block-protected-branch-work now worktree-aware) (#48)**
* docs: cross-ref git-workflow guide and refresh hook entries (block-memory-writes removed, block-protected-branch-work now worktree-aware)
```

With:
```
- **cross-ref git-workflow guide and refresh hook entries (block-protected-branch-work now worktree-aware) (#48)**
* docs: cross-ref git-workflow guide and refresh hook entries (block-protected-branch-work now worktree-aware)
```

Line 51 — delete:
```
- block-memory-writes: enforces auto-memory policy (no MEMORY.md writes)
```

- [ ] **Step 5: Fix `releases/v1.4.2.md`**

Lines 72 and 86 — both are `block-memory-writes` entries in hooks lists. Delete both lines:
```
- block-memory-writes: enforces auto-memory policy (no MEMORY.md writes)
```

- [ ] **Step 6: Fix `CHANGELOG.md`**

Line 201 — same pattern as the release notes. Replace:
```
- cross-ref git-workflow guide and refresh hook entries (block-memory-writes removed, block-protected-branch-work now worktree-aware) (#48)
```

With:
```
- cross-ref git-workflow guide and refresh hook entries (block-protected-branch-work now worktree-aware) (#48)
```

- [ ] **Step 7: Verify no remaining references**

Run: `grep -rn "block-memory-writes" --include="*.md" --include="*.json"`
Expected: Only the design spec `docs/specs/2026-05-01-memory-management-design.md` (which documents the removal decision).

- [ ] **Step 8: Run markdownlint on all changed files**

Run: `markdownlint CHANGELOG.md releases/v1.3.0.md releases/v1.3.1.md releases/v1.4.2.md docs/site/docs/hooks/index.md`
Expected: No errors.

- [ ] **Step 9: Commit**

```bash
git add CHANGELOG.md releases/v1.3.0.md releases/v1.3.1.md releases/v1.4.2.md docs/site/docs/hooks/index.md
st-commit --type chore --scope hooks --message "remove phantom block-memory-writes hook references" --agent claude
```

---

### Task 7: Create fleet rollout tracking issue and per-repo issues

**Files:**
- No file changes — GitHub issue creation via `gh`

- [ ] **Step 1: Enumerate the fleet**

Run: `gh repo list wphillipmoore --json name --jq '.[].name' --limit 100`

Identify all consuming repos that are part of the standard-tooling fleet. Exclude `standard-tooling-plugin` itself (the changes ship with this branch).

- [ ] **Step 2: Check which repos have existing memory files**

For each repo, check if memory files exist:
```bash
ls ~/.claude/projects/-Users-pmoore-dev-github-<repo-name>/memory/*.md 2>/dev/null | wc -l
```

Categorize repos into:
- **Has memory files** — needs init + audit + CLAUDE.md update
- **No memory files** — needs init + CLAUDE.md update (skip audit)

- [ ] **Step 3: Write the per-repo issue template**

Create a temp file with the issue body template. Two variants:

**Variant A — repo has existing memory files:**

```markdown
## Memory management rollout

Follow-up from wphillipmoore/standard-tooling-plugin#219.

### Steps

1. Run `/standard-tooling:memory-init` to plant the policy header in MEMORY.md.
2. Run `/standard-tooling:memory-audit` to review existing memory files.
3. Update this repo's CLAUDE.md — replace any "ban memory" or "auto-memory" policy text with the new managed approach, or remove it if the global policy and MEMORY.md header provide sufficient coverage.
4. Grep for `block-memory-writes` in this repo and remove any references.

### Context

Agents now require human approval before writing to memory. When proposing a memory write, the agent suggests a destination: repo memory, global CLAUDE.md, or a plugin/skill issue. The human confirms or redirects. See the design spec for full rationale: wphillipmoore/standard-tooling-plugin@docs/specs/2026-05-01-memory-management-design.md
```

**Variant B — no existing memory files:**

Same as Variant A but with step 2 replaced by: "*(Skip — no existing memory files to audit.)*"

- [ ] **Step 4: Create per-repo issues**

For each consuming repo:
```bash
gh issue create --repo wphillipmoore/<repo-name> --title "chore: memory management rollout (#219)" --body-file <tempfile>
```

Capture the issue URL for each.

- [ ] **Step 5: Create the fleet tracking issue in standard-tooling-plugin**

Write a temp file with the tracking issue body — a checklist of all consuming repos with links to their per-repo issues:

```markdown
## Fleet rollout tracker

Track memory management rollout across all consuming repos.
Per-repo issues created from #219.

- [ ] repo-name-1 — wphillipmoore/repo-name-1#NN
- [ ] repo-name-2 — wphillipmoore/repo-name-2#NN
...
```

```bash
gh issue create --repo wphillipmoore/standard-tooling-plugin --title "chore: fleet rollout tracker for memory management (#219)" --body-file <tempfile>
```

- [ ] **Step 6: Link the tracking issue to #219**

Add a comment on #219 referencing the tracking issue.
